# Open Questions

- Team member names are unknown; docs use TODO placeholders.
- Local SNAP dataset paths are unknown; default runs use synthetic graphs.
- CUDA toolkit `nvcc` is not available on PATH in the current environment.
- `nvidia-smi` detects an NVIDIA GeForce RTX 3060 Laptop GPU with 6144 MiB VRAM, but Numba CUDA reports `cuda_available: false`.
- PowerShell activation from the UNC workspace was blocked by execution policy; commands use `.venv\Scripts\python.exe` directly.
