# Open Questions

- Local SNAP dataset paths are unknown; default runs use synthetic graphs.
- Real SNAP datasets have not been downloaded yet because they are large; use `docs/dataset_setup.md` when needed.
- PowerShell activation from the UNC workspace can be blocked by execution policy; commands use `.venv\Scripts\python.exe` directly or `powershell -ExecutionPolicy Bypass -File ...`.
